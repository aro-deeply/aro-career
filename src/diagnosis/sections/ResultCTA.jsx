import React from "react";

// This component is superseded by the inline CTA block in ResultStep.jsx.
// Kept as a file stub to avoid breaking any future imports.
export default function ResultCTA({ onReset }) {
  return null;
}
